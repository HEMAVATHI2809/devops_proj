# Browser calls same origin + /api → nginx in frontend container proxies to backend (Docker).
# For local `npm start`, package.json "proxy" forwards /api to http://localhost:5000.
REACT_APP_API_URL=/api
